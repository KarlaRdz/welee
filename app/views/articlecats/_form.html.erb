<%= form_with(model: articlecat, local: true) do |form| %>
  <% if articlecat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(articlecat.errors.count, "error") %> prohibited this articlecat from being saved:</h2>

      <ul>
      <% articlecat.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :articlecat_name %>
  </div>

  <div class="field">
    <%= form.label :video %>
    <%= form.text_field :video, id: :articlecat_video %>
  </div>

  <div class="field">
    <%= form.label :active %>
    <%= form.number_field :active, id: :articlecat_active %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
