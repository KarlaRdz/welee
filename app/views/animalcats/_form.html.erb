<%= form_with(model: animalcat, local: true) do |form| %>
  <% if animalcat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(animalcat.errors.count, "error") %> prohibited this animalcat from being saved:</h2>

      <ul>
      <% animalcat.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :animalcat_name %>
  </div>

  <div class="field">
    <%= form.label :video %>
    <%= form.text_field :video, id: :animalcat_video %>
  </div>

  <div class="field">
    <%= form.label :active %>
    <%= form.number_field :active, id: :animalcat_active %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
